// Configuration Constants
const CONFIG = {
    API_URL: 'https://api.somnia.com',
    TIMEOUT: 5000,
    RETRY_COUNT: 3,
};

// Tone Options
const TONE_OPTIONS = {
    POSITIVE: 'positive',
    NEUTRAL: 'neutral',
    NEGATIVE: 'negative',
};

module.exports = { CONFIG, TONE_OPTIONS };